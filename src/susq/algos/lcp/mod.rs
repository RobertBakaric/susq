pub mod kasai;
//pub mod karkkainen;

/*
#[derive(Debug, Fail)]
pub enum Error {
    /// An error occurred while uploading the file.
    #[fail(display = "")]
    Lcp(#[cause] LcpError),
}


impl From<LcpError> for Error {
    fn from(err: LcpError) -> Error {
        Error::Lcp(err)
    }
}
*/
